#include "double.hpp"

prec_double::prec_double(const std::string& num,
                         const std::string& denom) :
                        num(num),
                        denom(denom) {};
